building_mcr_cyber_replicators = {
	base_buildtime = @b1_time

	category = pop_assembly

	base_cap_amount = 1

	prerequisites = { "tech_cloning" }

	potential = {
		exists = owner
		owner = { has_trait = trait_mechroid }
		NOT = { has_modifier = resort_colony }
		OR = {
			owner = { is_ai = no }
			NOT = { has_building = building_robot_assembly_plant }
		}
	}

	destroy_trigger = {
		exists = owner
		OR = {
			AND = {
				owner = { is_ai = yes }
				planet = {
					free_district_slots = 0
					free_building_slots = 0
				}
			}
			NOT = { owner = {  has_trait = trait_mechroid } }
		}
	}

	planet_modifier = {
		planet_pop_assembly_organic_add = 2
		job_bio_assembler_add = 2
	}

	resources = {
		category = planet_buildings
		cost = {
			minerals = 500
		}
		upkeep = {
			energy = 5
			food = 20
		}
	}

	triggered_desc = {
		trigger = {
			has_building = building_robot_assembly_plant
		}
		text = tooltip_organic_and_artificial_assembly_warning
	}
}